import { GalleryTabImgPipe } from './gallery-tab-img.pipe';

describe('GalleryTabImgPipe', () => {
  it('create an instance', () => {
    const pipe = new GalleryTabImgPipe();
    expect(pipe).toBeTruthy();
  });
});
