import { Routes } from '@angular/router';
import { ResumeContainerComponent } from './lib/components/resume-container/resume-container.component';

export const resumeRoutes: Routes = [
  { path: '', component: ResumeContainerComponent }
];
